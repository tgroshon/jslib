/**
 * Top-level Parent
 */
export class Dropdown extends HTMLElement {
  constructor() {
    // Always call super first in constructor
    super();

    // write element functionality in here
  }
}

/**
 * Triggering button that toggles the dropdown open or closed
 */
export class DropdownToggle extends HTMLElement {
  constructor() {
    // Always call super first in constructor
    super();

    // write element functionality in here
  }
}

/**
 * Container for dropdown list items
 */
export class DropdownMenu extends HTMLElement {
  constructor() {
    // Always call super first in constructor
    super();

    // write element functionality in here
  }
}

/**
 * Items in the dropdown list
 */
export class DropdownItem extends HTMLElement {
  constructor() {
    // Always call super first in constructor
    super();

    // write element functionality in here
  }
}
