/**
 * Transition: add enter/leave transitions to conditionally rendered elements,
 * using CSS classes to control the actual transition styles in the different
 * stages of the transition.
 *
 * Accepts a `show` prop that controls whether the children should be shown or
 * hidden, and a set of lifecycle props (like `enterFrom`, and `leaveTo`) that let
 * you add CSS classes at specific phases of a transition.
 */
export class Transition extends HTMLElement {
  constructor() {
    // Always call super first in constructor
    super();

    // write element functionality in here
  }
}
